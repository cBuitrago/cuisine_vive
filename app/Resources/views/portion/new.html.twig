{% extends 'base.html.twig' %}

{% block body %}
    <h1>Portion creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {% for language in form.languages %}
        <li>{{ form_row(language) }}</li>
        {% endfor %}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('portion_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
