{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div class="contenu">
        <h1>Créér Catégorie</h1>
        <div class="form-horizontal">
            <div class="form-group">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                
                <input type="submit" value="Créér catégorie" class="btn btn-default"/>
                {{ form_end(form) }}
            </div>
        </div>
        <ul>
            <li>
                <a href="{{ path('category_index') }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-arrow-left" aria-hidden="true"></span>Retourner à la liste
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
