{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <div class="contenu">

        <h1>Icons list</h1>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for icon in icons %}
                    <tr>
                        <td><a href="{{ path('icon_show', { 'id': icon.id }) }}">{{ icon.id }}</a></td>
                        <td>{{ icon.name }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('icon_show', { 'id': icon.id }) }}">
                                        <span class="glyphicon glyphicon-play-circle" aria-hidden="true"></span>
                                    </a>
                                </li>
                                <li>
                                    <a href="{{ path('icon_edit', { 'id': icon.id }) }}">
                                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                                    </a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>

        <ul>
            <li>
                <a href="{{ path('icon_new') }}" class="btn btn-default">
                    <span class="glyphicon glyphicon-star" aria-hidden="true"></span>Create a new icon
                </a>
            </li>
        </ul>
    </div>
{% endblock %}
