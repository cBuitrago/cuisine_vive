{% extends 'base.html.twig' %}

{% block body %}
    <h1>Portionlanguage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ portionLanguage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ portionLanguage.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ portionLanguage.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('portionlanguage_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('portionlanguage_edit', { 'id': portionLanguage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
