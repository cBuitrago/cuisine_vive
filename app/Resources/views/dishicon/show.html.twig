{% extends 'base.html.twig' %}

{% block body %}
    <h1>Dishicon</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dishIcon.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('dishicon_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('dishicon_edit', { 'id': dishIcon.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
