{% extends 'base.html.twig' %}

{% block body %}
    <h1>Categorylanguage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoryLanguage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categoryLanguage.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categoryLanguage.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('categorylanguage_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('categorylanguage_edit', { 'id': categoryLanguage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
